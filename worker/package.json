{
  "name": "worker",
  "version": "1.0.0",
  "description": "Background worker: polls the HN API and generates article summaries via a local Ollama model",
  "license": "ISC",
  "author": "",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "start": "node --env-file-if-exists=../.env index.js",
    "fetch:once": "node --env-file-if-exists=../.env scripts/fetch-once.js",
    "summarize:once": "node --env-file-if-exists=../.env scripts/summarize-once.js",
    "retry-failed": "node --env-file-if-exists=../.env scripts/retry-failed.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "db": "*",
    "@mozilla/readability": "^0.5.0",
    "jsdom": "^25.0.1",
    "playwright": "^1.48.2"
  },
  "devDependencies": {}
}
